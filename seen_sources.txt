# Scout Smart Memory Log
# This file tracks ALL documents reviewed by the AI Scout Agent (HIGH, MEDIUM, and LOW relevance).
# It serves as the agent's memory to ensure it never evaluates the same paper twice.
# Format -> link|date
